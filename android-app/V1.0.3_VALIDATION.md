# Android v1.0.3 validation

This branch exists only to run the release validation workflow against the hardened v1.0.3 source already present on `main`.

The workflow verifies API 23 URL interception, safe downloads, disabled third-party cookies, verified App Links, lifecycle cleanup, non-debuggable release output and AndroidX version metadata.
