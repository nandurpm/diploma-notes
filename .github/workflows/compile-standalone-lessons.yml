name: Disabled - Compile standalone lesson handbooks

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow was disabled to stop unnecessary GitHub Actions runs and cache creation."
