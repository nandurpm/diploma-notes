/* Legacy compatibility shim. Mock-exam grading is server-side only. */
(() => {
  "use strict";
  // Older cached shells may still request this asset; intentionally do not evaluate answers here.
})();
