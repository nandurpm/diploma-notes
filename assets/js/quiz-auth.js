window.PolyQuizAuth=(()=>{let client=null,user=null,guest=false;const meta=n=>document.querySelector(`meta[name="${n}"]`)?.content||'';function getClient(){if(client)return client;if(window.supabase?.createClient){const url=meta('supabase-url'),key=meta('supabase-publishable-key');if(url&&key)client=window.supabase.createClient(url,key,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}})}return client}async function profile(u,name){let fallback=name||u.user_metadata?.username||u.email?.split('@')[0]||'student',db=getClient();if(!db)return fallback;try{let r=await db.from('profiles').select('username').eq('id',u.id).maybeSingle();if(r.data?.username)return r.data.username;await db.from('profiles').upsert({id:u.id,username:fallback})}catch{}return fallback}async function login(email,password){let db=getClient();if(!db)throw Error('Login system did not load. Continue as Guest or check internet.');let res=await db.auth.signInWithPassword({email,password});if(res.error)throw res.error;user=res.data.user;guest=false;return{user,name:await profile(user)}}async function register(username,email,password,confirm){if(!username||!email||!password||password!==confirm)throw Error('Check username, email, password and confirmation.');let db=getClient();if(!db)throw Error('Registration system did not load.');let res=await db.auth.signUp({email,password,options:{data:{username}}});if(res.error)throw res.error;if(res.data?.session?.user){user=res.data.user;guest=false;return{user,name:await profile(user,username)}}return{pending:true,message:'Registered. Check email if confirmation is enabled, then login.'}}function asGuest(){guest=true;user=null;return{name:'Guest'}}async function logout(){let db=getClient();if(!guest&&db)await db.auth.signOut();user=null;guest=false}async function restore(){let db=getClient();if(!db)return null;try{let{data}=await db.auth.getSession();if(data?.session?.user){user=data.session.user;guest=false;return{user,name:await profile(user)}}}catch{}return null}return{getClient,login,register,asGuest,logout,restore,get user(){return user},get guest(){return guest}}})();
