(() => { "use strict"; const VERSION = "20260716-static-pages"; const esc = value => String(value ?? "").replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&#039;"); const slug = value => String(value || "").toLowerCase().replace(/&/g, " and ").replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, ""); const semRank = value => Number(String(value || "").match(/\d+/)?.[0] || 999); async function json(url, timeoutMs = 12000) { const controller = new AbortController(); const timeout = setTimeout(() => controller.abort(), timeoutMs); try { const response = await fetch(url, { cache: "no-store", signal: controller.signal }); if (!response.ok) throw new Error(`HTTP ${response.status}`); return await response.json(); } finally { clearTimeout(timeout); } } const cleanPath = () => location.pathname.replace(/\/+$/, "") || "/"; const isProgrammeIndex = () => ["/revision-2026.html", "/revision-2026"].includes(cleanPath()); function departmentSlug() { if (document.body?.dataset?.programmeSlug) return document.body.dataset.programmeSlug; const querySlug = new URLSearchParams(location.search).get("dept"); if (querySlug) return querySlug.replace(/\.html$/i, ""); const match = cleanPath().match(/\/revision-2026\/([^/]+)\.html$/i); return match ? decodeURIComponent(match[1]) : ""; } function enhanceProgrammeIndex() { const grid = document.getElementById("departmentCards"); const search = document.getElementById("programmeSearch"); const empty = document.getElementById("programmeEmptyState"); if (!grid || !search) return; const cards = [...grid.querySelectorAll("[data-programme-card]")]; const draw = () => { const query = search.value.trim().toLowerCase(); let visible = 0; cards.forEach(card => { const haystack = [card.dataset.programmeSlug, card.dataset.officialCode, card.textContent].join(" ").toLowerCase(); const show = !query || haystack.includes(query); card.hidden = !show; if (show) visible += 1; }); if (empty) empty.hidden = visible !== 0; }; search.addEventListener("input", draw); draw(); } function subjectCard(subject) { const code = esc(subject.code); const name = esc(subject.name); const semester = esc(subject.semester || "Semester not specified"); const type = esc(subject.type || "Course"); const syllabus = esc(subject.syllabusUrl || `https://www.sitttrkerala.ac.in/index.php?r=site%2Fdiploma-syllabus-course-contents&course=${encodeURIComponent(subject.code)}`); const handbook = `/lessons/lessons-${encodeURIComponent(subject.code)}_REV2026.html`; return `<article class="subject-card reveal"><div class="subject-top"><span>REV2026</span><strong>${code}</strong></div><h3>${name}</h3><p>${semester} / ${type}</p><div class="action-row"><a class="action syllabus" href="${syllabus}" target="_blank" rel="noopener noreferrer">Open Syllabus</a><a class="action lessons" href="${handbook}">View Handbook</a></div></article>`; } async function renderSubjects() { const grid = document.getElementById("subjectGrid"); if (!grid) return; const dept = departmentSlug(); const title = document.getElementById("departmentTitle"); const crumb = document.getElementById("crumbDepartment"); const summary = document.getElementById("departmentSummary"); const semesterSelect = document.getElementById("semesterFilter"); const search = document.getElementById("subjectSearch"); if (!dept) { grid.innerHTML = '<div class="empty-state">No Revision 2026 department was selected. Return to the department list.</div>'; if (summary) summary.textContent = "No department was selected."; return; } try { const [programmes, data] = await Promise.all([ json(`/assets/data/revision-2026-programmes.json?v=${VERSION}`), json(`/assets/data/revision-2026-subjects.json?v=${VERSION}`, 20000) ]); const programme = programmes.programmes.find(item => item.slug === dept); const programmeName = programme?.name || document.body.dataset.programmeName || "Revision 2026 subjects"; if (title) title.textContent = programmeName; if (crumb) crumb.textContent = programmeName; document.title = `${programmeName} Revision 2026 Subjects | POLY PMNA`; const all = (data.subjects || []).filter(subject => subject.programmeSlug === dept || slug(subject.programme) === dept); if (summary) summary.textContent = all.length ? `${all.length} official subject records are indexed for ${programmeName}.` : `No verified Revision 2026 subject records are currently indexed for ${programmeName}.`; if (semesterSelect) { [...new Set(all.map(subject => subject.semester).filter(Boolean))] .sort((a, b) => semRank(a) - semRank(b) || String(a).localeCompare(String(b))) .forEach(value => semesterSelect.append(Object.assign(document.createElement("option"), { value, textContent: value }))); } const draw = () => { const query = (search?.value || "").trim().toLowerCase(); const semester = semesterSelect?.value || "all"; const list = all .filter(subject => semester === "all" || subject.semester === semester) .filter(subject => [subject.code, subject.name, subject.type].join(" ").toLowerCase().includes(query)) .sort((a, b) => semRank(a.semester) - semRank(b.semester) || String(a.code).localeCompare(String(b.code), undefined, { numeric: true })); grid.innerHTML = list.length ? list.map(subjectCard).join("") : '<div class="empty-state">No verified Revision 2026 subjects match this filter.</div>'; }; search?.addEventListener("input", draw); semesterSelect?.addEventListener("change", draw); draw(); } catch (error) { console.error("REV2026 subject loading failed", error); if (summary) summary.textContent = "The subject dataset could not be loaded. Use the official SITTTR syllabus link below and reload this page later."; grid.innerHTML = '<div class="empty-state">Revision 2026 subjects could not be loaded. <a href="https://www.sitttrkerala.ac.in/index.php?r=site%2Fdiploma-syllabus&scheme=REV2026" target="_blank" rel="noopener noreferrer">Open the official SITTTR Revision 2026 page</a>.</div>'; } } const start = () => isProgrammeIndex() ? enhanceProgrammeIndex() : renderSubjects(); if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", start, { once: true }); else start(); })();