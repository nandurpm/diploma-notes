{
  "name": "ask-poly-ai-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js",
    "test": "node --test test/*.test.js test/*.test.mjs",
    "coverage": "node --test --experimental-test-coverage test/*.test.js test/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.131.0"
  }
}
